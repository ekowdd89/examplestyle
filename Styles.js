import React, { Comoponents } from "react";
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        flex:1,
        backgroundColor:'red',
        justifyContent:'center',
        alignContent:'center',
        alignItems:'center'
    },
    /// change text color
    txt:{
        color:'#fff',
        fontSize:20,
        fontWeight:'bold'
    }
})